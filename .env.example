"""Optional local environment configuration for the free-only backend.

Copy these values into the host environment when running `main.py`. Leave REDIS_URL
empty for simple local operation; the bounded memory cache still works without Redis.
"""
REDIS_URL=
ALLOWED_ORIGINS=http://localhost:8000
CACHE_TTL_SECONDS=600
ALERT_TTL_SECONDS=300
