services:
  - type: web
    name: myanmar-weather-api
    env: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: ALLOWED_ORIGINS
        sync: false
      - key: REDIS_URL
        sync: false
